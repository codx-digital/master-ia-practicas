# CP5 — RL en highway-env
# Versiones pinneadas con wheels para Python 3.11 / 3.12 / 3.13 / 3.14 (CPU).
# Validado en macOS Apple Silicon con Python 3.14 (DQN entrena + eval) — 2026-07-23.
# Nota: instalar la versión MÁS RECIENTE de Python (3.14) funciona; también 3.11 / 3.12 / 3.13.

# Core
numpy==2.5.1

# Deep learning + RL (CPU)
torch==2.13.0
stable-baselines3==2.9.0

# Simulación (highway-env 1.12 + gymnasium 1.x + pygame 2.6 → instalan en 3.14)
gymnasium==1.3.0
highway-env==1.12.0
pygame==2.6.1

# Visualización
matplotlib==3.11.1
imageio==2.37.4          # para generar GIFs de evaluación
ipywidgets==8.1.8

# Jupyter
jupyter==1.1.1
ipykernel==7.3.0
notebook==7.6.0

# Utilidades
tqdm==4.69.0
rich==15.0.0             # requerido por progress_bar=True de stable-baselines3
pandas==3.0.3

# Opcional para vídeos MP4 (necesita ffmpeg en el sistema):
# moviepy
