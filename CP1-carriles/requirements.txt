# CP1 — Detección de Carriles
# Versiones pinneadas, validadas en CPU (macOS Apple Silicon + Linux x86_64 + Windows)
# Última verificación: 2026-02-XX (actualizar antes de cada sesión)

# Core
numpy==1.26.4
pillow==10.3.0

# CV clásica
opencv-python==4.9.0.80

# Deep learning (CPU)
torch==2.3.0
torchvision==0.18.0
onnxruntime==1.17.3      # Inferencia del modelo de carriles en formato ONNX (CPU-only)

# Visualización
matplotlib==3.8.4
ipywidgets==8.1.2

# Jupyter
jupyter==1.0.0
ipykernel==6.29.4
notebook==7.1.3

# Utilidades
tqdm==4.66.4
pyyaml==6.0.1
requests==2.31.0
scipy==1.13.0            # polyfit en post-procesado DL y extensiones

# Opcional para extensiones (descomentar si se usa):
# scikit-image==0.23.2   # filtros avanzados
