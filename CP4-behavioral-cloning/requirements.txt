# CP4 — Behavioral Cloning
# Versiones pinneadas con wheels para Python 3.11 / 3.12 / 3.13 / 3.14 (CPU).
# Validado en macOS Apple Silicon con Python 3.14 (dataset + notebooks) — 2026-07-21.
# Nota: instalar la versión MÁS RECIENTE de Python (3.14) funciona; también 3.11 / 3.12 / 3.13.

# Core
numpy==2.5.1
scipy==1.18.0

# Deep learning (CPU)
torch==2.13.0
torchvision==0.28.0

# Simulación (mismo entorno que CP5). highway-env 1.12 + gymnasium 1.x + pygame 2.6
# instalan en 3.14 (pygame compila desde fuente si no hay wheel).
highway-env==1.12.0
gymnasium==1.3.0
pygame==2.6.1            # backend de renderizado (highway-env también usa pygame-ce)

# Visualización
matplotlib==3.11.1
pillow==12.3.0
ipywidgets==8.1.8

# Jupyter
jupyter==1.1.1
ipykernel==7.3.0
notebook==7.6.0

# Utilidades
tqdm==4.69.0
pandas==3.0.3

# Opcional extensión:
# imageio                  # Para generar GIFs de rollouts
# opencv-python==4.13.0.92 # Si quieres data augmentation avanzada
